{
  "name": "pollis",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "GODEBUG=asyncpreemptoff=1 WEBKIT_DISABLE_COMPOSITING_MODE=1 GOTRACEBACK=all ./scripts/dev-wails.sh",
    "dev:frontend": "turbo run dev --filter=frontend",
    "dev:server": "cd server && go run cmd/server/main.go -port=${PORT:-50051}",
    "build": "turbo run build",
    "build:app": "wails build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "proto": "make -C . proto",
    "test": "node scripts/e2e-test.js",
    "test:e2e": "node scripts/e2e-test.js"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">=18"
  }
}

