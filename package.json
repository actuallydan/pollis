{
  "name": "pollis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "WEBKIT_DISABLE_COMPOSITING_MODE=1 GDK_BACKEND=x11 pnpm tauri dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "sh -c 'if [ -f .env.development ]; then set -a; . .env.development; set +a; fi; pnpm tauri build'",
    "build:linux": "sh -c 'if [ -f .env.development ]; then set -a; . .env.development; set +a; fi; pnpm tauri build --target x86_64-unknown-linux-gnu'",
    "build:macos": "sh -c 'if [ -f .env.development ]; then set -a; . .env.development; set +a; fi; pnpm tauri build --target universal-apple-darwin'",
    "build:windows": "sh -c 'if [ -f .env.development ]; then set -a; . .env.development; set +a; fi; pnpm tauri build --target x86_64-pc-windows-msvc'",
    "lint": "pnpm --filter frontend lint",
    "clean": "pnpm --filter frontend clean && cargo clean --manifest-path src-tauri/Cargo.toml",
    "secrets:decrypt": "sops --decrypt --input-type dotenv --output-type dotenv secrets.enc.env > .env.development",
    "secrets:encrypt": "sops --encrypt --filename-override secrets.enc.env --input-type dotenv --output-type dotenv .env.development > secrets.enc.env",
    "secrets:edit": "sops --filename-override secrets.enc.env --input-type dotenv --output-type dotenv secrets.enc.env",
    "db:push": "cargo run --manifest-path src-tauri/Cargo.toml --bin migrate --features pollis/migrate-bin",
    "test:e2e": "playwright test",
    "test:e2e:interactive": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@tauri-apps/cli": "^2",
    "wrangler": "^4.76.0"
  },
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">=18"
  }
}
