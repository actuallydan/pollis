# Middleware

## custom
- migrate — `scripts/db-apply.sh` (runs against Turso during release; reads from `pollis-schema/migrations/`)

## auth
- auth — `pollis-core/src/commands/auth.rs`
- auth.spec — `tests/e2e/auth.spec.ts`
