<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSUserNotificationAlertStyle</key>
	<string>alert</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>Pollis needs microphone access for voice channels</string>
	<!-- Required for screen sharing via ScreenCaptureKit on macOS 12.3+.
	     Note: TCC permission for screen recording does not inherit from
	     the parent process to subprocess helpers, so the actual capture
	     helper binary will need its own bundle and Info.plist with the
	     same key set when the macOS path lands. -->
	<key>NSScreenCaptureUsageDescription</key>
	<string>Pollis needs screen recording access to share your screen in voice channels</string>
</dict>
</plist>
