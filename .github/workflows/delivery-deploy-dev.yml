# Deploy the Delivery Service to DEV — Cloudflare Containers (#515).
#
# Replaces the old GHCR-build + Watchtower-poke path (which silently no-op'd for
# 11 days). Now: sync secrets Doppler -> Wrangler Secrets Store, then
# `wrangler deploy` (versioned, health-gated), then verify the running /version
# reports the built SHA. Dev auto-deploys on merge to main touching
# pollis-delivery/** — safe because dev and prod are SEPARATE wrangler configs,
# so this can never touch prod.
name: Deploy Delivery (dev)

on:
  push:
    branches: [main]
    paths:
      - "pollis-delivery/**"
      - ".github/workflows/delivery-deploy-dev.yml"
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to build (branch, tag, or SHA)
        required: false
        default: main

permissions:
  contents: read

concurrency:
  group: delivery-deploy-dev
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: delivery-dev
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      # Account's single Secrets Store; dev secrets namespaced with DS_DEV_.
      STORE_ID: ${{ vars.SECRETS_STORE_ID }}
      SECRET_PREFIX: DS_DEV_
      WRANGLER_CONFIG: wrangler.dev.jsonc
      VERSION_URL: https://api-dev.pollis.com/version
    steps:
      - name: Checkout ${{ inputs.ref || github.sha }}
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.sha }}

      - name: Resolve build SHA
        id: sha
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install Worker toolchain
        run: pnpm install --filter pollis-delivery-worker

      - name: Install Doppler CLI
        uses: dopplerhq/cli-action@v3

      # Doppler is the single source of truth. Pull the DS config and upsert each
      # key into Secrets Store under the DS_DEV_ prefix. DOPPLER_TOKEN is a
      # service token scoped to pollis/dev_personal (set in the delivery-dev env).
      - name: Sync secrets Doppler -> Secrets Store
        working-directory: pollis-delivery
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}
          INCLUDE_DEV_OTP: "true"
        run: bash ../.github/scripts/sync-ds-secrets.sh

      - name: Stamp GIT_SHA into container build args
        working-directory: pollis-delivery
        run: |
          sed -i "s|\"GIT_SHA\": \"unknown\"|\"GIT_SHA\": \"${{ steps.sha.outputs.sha }}\"|" "$WRANGLER_CONFIG"
          grep -n '"GIT_SHA"' "$WRANGLER_CONFIG"

      - name: Deploy (wrangler)
        working-directory: pollis-delivery
        run: pnpm exec wrangler deploy -c "$WRANGLER_CONFIG"

      # Do NOT trust "deployed OK" — confirm the new binary is actually SERVING.
      # Poll /version until it reports the SHA we just built; fail otherwise. This
      # is the #509 tripwire, target-agnostic and kept verbatim.
      - name: Verify new image is live
        run: |
          EXPECTED='${{ steps.sha.outputs.sha }}'
          echo "Waiting for $VERSION_URL to report sha=$EXPECTED"
          for i in $(seq 1 40); do
            GOT="$(curl -fsS -m 10 "$VERSION_URL" | jq -r '.sha' 2>/dev/null || echo '')"
            if [ "$GOT" = "$EXPECTED" ]; then echo "✓ live sha=$GOT"; exit 0; fi
            echo "[$((i*6))s] live sha=$GOT (want $EXPECTED)"; sleep 6
          done
          echo "::error::Delivery (dev) did not report the new SHA within 240s — the deploy did not take effect."
          exit 1
