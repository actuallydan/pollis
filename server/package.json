{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run cmd/server/main.go -port=${PORT:-50051}",
    "build": "go build -o bin/server cmd/server/main.go",
    "lint": "echo 'Linting server...' && golangci-lint run ./... || true",
    "clean": "rm -rf bin"
  }
}

