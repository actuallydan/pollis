WEBVTT

NOTE Captions for "pollis-verify: verify it yourself" (Topic 11). No audio track
NOTE yet by decision; these captions carry the narration. Source of truth:
NOTE learn/manim/scripts/verify-it-yourself.md

00:00.000 --> 00:15.000
Everything we've talked about is checkable. Here's how, in about two minutes. First, the API — and it's barely an API. It's a shelf of static files. No database, no logic, nothing that can behave differently depending on who's asking.

00:15.000 --> 00:30.000
That's on purpose. A server that computes an answer can compute a different one for you. A server handing out files it wrote in advance has a much harder time.

00:30.000 --> 00:45.000
You can fetch any of them right now. Here's the current signed head — root, entry count, signature.

00:45.000 --> 01:00.000
And here's the same root on our website. Same sixty-four characters. Two different places.

01:00.000 --> 01:15.000
But reading files only tells you what we claim. To check the claims, use the verifier. It's a small program you download — or build yourself, and if you're seriously auditing us, build it.

01:15.000 --> 01:30.000
It re-hashes every entry, rebuilds the tree, checks every proof, and verifies the signature against a key compiled into it. Not fetched. Compiled in. One command.

01:30.000 --> 01:50.000
Watch what comes back. Tree size, sixty. The root — the same one we just fetched. Then every artifact in the release, each with a tick. Those ticks aren't a status we wrote down: each one is an inclusion proof your machine just recomputed. And PASS means the whole tree replayed cleanly.

01:50.000 --> 02:20.000
Speaking of which. Suppose we served you our own signing key, with a perfect fake log to match. The verifier compares it to the key built into it — and stops.

02:20.000 --> 02:35.000
One last thing, and it's the most important use of this tool. Two people, different countries, different networks, same command. Same root. Same PASS.

02:35.000 --> 02:45.000
If those ever differed — two valid signatures over two different trees — that's the finding that matters. It's also the only one you can't get alone. So run it. And compare with someone.
